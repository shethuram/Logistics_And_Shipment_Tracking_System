# Project Rules

## Git Workflow
- Always commit work on the active feature branch (e.g. `feature/frontend-setup`).
- Do NOT merge the feature branch locally into `main`.
- Leave the local `main` branch clean and matching the remote `origin/main` so that the developer can push the feature branch and create a Pull Request on the remote repository.
